import 'package:flutter/material.dart';

class ActivityViewerPage extends StatelessWidget {
  final String id;
  const ActivityViewerPage({
    super.key,
    required this.id,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
